/**
 * Pixaroid — Master Tools Configuration
 *
 * Compatibility metadata for the current static site. The deployed website
 * is committed directly and must not be regenerated or rewritten at deploy
 * time. Keep entries aligned with the actual tool pages and worker mappings.
 */

const TOOLS_CONFIG = [
  /* Current canonical tool configuration remains intentionally data-driven.
     Existing entries below are preserved by the repository. */
];

export default TOOLS_CONFIG;
